{
  "name": "nuxt-app",
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "export": "nuxt export"
  },
  "devDependencies": {
    "@types/node": "^18",
    "nuxt": "^3.4.1"
  }
}
